import Api from './Api'
import ui from '../ui'

class PhotoFrame {
  constructor(pastel, frame, canvas) {
    this.pastel = pastel
    this.frame = frame

    this.init()
  }

  init() {}
}

export default PhotoFrame
